package model.gamemanagement;

/**
 * Enum for status when user clicks on gamboard. Used to process the input to
 * the belonging action.
 *
 * @author Markus Mogck
 */
public enum EClickOnGameboardStatus
{

    NEW_POSITION,
    NEW_ATTACK,
    NEW_ACTION,
    GET_INFO;
}
